# wezterm-config

Clone the repo and set symlink

```nu
symlink ~/src/wezterm-config ~/.config/wezterm
```
Instead of symlink, environment variables `WEZTERM_CONFIG_DIR` or `WEZTERM_CONFIG_FILE` can control which config to load.
